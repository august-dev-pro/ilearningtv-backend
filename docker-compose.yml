version: '3'
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: seleka@13
      POSTGRES_DB: ilearningtv
    ports:
      - "5432:5432"